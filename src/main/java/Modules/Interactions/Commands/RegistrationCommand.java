package Modules.Interactions.Commands;

public enum RegistrationCommand implements Command {
    showCurrentMenu,
    register,
    askForPassword,
    askQuestion,
    login,
    forgetPassword,
    answerQuestion,
}
